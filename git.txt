==  Setup
    git config --global push.default current
        --  set the default push behavior to current branch
    git config --global user.email "matheus.mab432@gmail.com"
        --  set the global user.email 
    git config --global user.name "Matheus Bastos"
        --  set the global user.name